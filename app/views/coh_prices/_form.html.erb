<%= form_for(coh_price) do |f| %>
  <% if coh_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coh_price.errors.count, "error") %> prohibited this coh_price from being saved:</h2>

      <ul>
      <% coh_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="field">
    <%= f.label :package_id %>
    <%= f.text_field :package_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
